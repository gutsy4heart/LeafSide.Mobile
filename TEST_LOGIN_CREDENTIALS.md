# 🔐 Тестовые учетные данные

## 👤 Существующий тестовый пользователь:

```
Email: admin@leafside.local
Password: Admin12345!
Роль: Admin
```

Этот пользователь создается автоматически при первом запуске backend.

---

## 🧪 Как протестировать авторизацию:

### 1. Используйте существующего пользователя

В экране логина введите:
```
Email: admin@leafside.local
Password: Admin12345!
```

### 2. Или создайте нового пользователя

Перейдите на экран регистрации и создайте новый аккаунт:
```
Email: test@example.com
Password: Test123!
Имя: Иван
Фамилия: Иванов
```

---

## 🐛 Возможные причины ошибки 401:

### 1. **Неправильные учетные данные**
- Проверьте email и password
- Email должен быть валидным
- Password должен соответствовать требованиям

### 2. **Пользователь не существует**
- Сначала зарегистрируйтесь
- Или используйте admin@leafside.local

### 3. **Backend не работает**
Проверьте в терминале:
```bash
netstat -ano | findstr :5233
```

### 4. **База данных не инициализирована**
При первом запуске backend должен:
- Применить миграции
- Создать роли (Admin, User)
- Создать админ пользователя

---

## 🔍 Отладка:

### В логах Metro должно быть:

**При успешном логине:**
```
LOG  [AUTH] Login request body: {
  "Email": "admin@leafside.local",
  "Password": "Admin12345!"
}
LOG  [API] POST http://192.168.1.69:5233/api/account/login
LOG  [API] Success: OK
```

**При ошибке:**
```
LOG  [AUTH] Login request body: { ... }
LOG  [API] POST http://192.168.1.69:5233/api/account/login
ERROR [API] Error 401: {"status": 401, ...}
```

---

## 📋 Требования к паролю:

Backend (ASP.NET Identity) требует:
- Минимум 6 символов
- Минимум 1 заглавная буква
- Минимум 1 цифра
- Минимум 1 специальный символ (опционально)

**Примеры правильных паролей:**
- `Admin12345!` ✅
- `Test123!` ✅
- `Password1` ✅
- `User@123` ✅

**Примеры неправильных:**
- `test` ❌ (слишком короткий)
- `testtest` ❌ (нет заглавных и цифр)
- `TEST123` ❌ (нет маленьких букв)

---

## 🎯 Быстрый тест:

1. Откройте приложение
2. Перейдите на экран входа
3. Введите:
   ```
   admin@leafside.local
   Admin12345!
   ```
4. Нажмите "Войти"
5. Должен быть успех!

---

## 🔄 Если всё ещё ошибка:

### Проверьте логи backend:

В терминале где запущен backend должны быть логи.

### Проверьте формат запроса:

В Metro логах должно быть:
```
LOG  [AUTH] Login request body: {
  "Email": "...",      ← С заглавной буквы!
  "Password": "..."    ← С заглавной буквы!
}
```

Если там маленькие буквы - значит изменения не применились.

---

## 💡 Альтернативное решение:

Если ничего не помогает, можно создать нового пользователя напрямую через Swagger:

1. Откройте `http://192.168.1.69:5233/swagger`
2. Найдите `POST /api/account/register`
3. Создайте пользователя
4. Используйте его для входа

---

## 🎉 Готово!

Используйте учетные данные выше для тестирования! 🔐

