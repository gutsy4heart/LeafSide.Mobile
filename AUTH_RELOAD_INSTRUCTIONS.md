# 🔄 Инструкция по применению исправлений авторизации

## ✅ Что было исправлено:

Исправлена ошибка **401 Unauthorized** при логине/регистрации.

**Файл:** `src/services/auth.ts`

Теперь данные отправляются в формате PascalCase, который ожидает backend:
- `Email` вместо `email`
- `Password` вместо `password`
- `FirstName` вместо `firstName`
- и т.д.

---

## 📱 Как применить изменения:

### ✨ Metro bundler перезапущен!

QR код обновлён. Теперь нужно:

### Вариант 1: Полная перезагрузка (рекомендуется)

1. **Закройте Expo Go полностью** на iPhone
   - Свайп вверх в списке приложений
   - Закройте Expo Go

2. **Откройте Expo Go снова**

3. **Отсканируйте новый QR код** с экрана компьютера

### Вариант 2: Быстрая перезагрузка

1. **В открытом приложении:**
   - Встряхните iPhone
   - Нажмите **"Reload"**

### Вариант 3: Через Metro

В терминале на компьютере нажмите **`r`** (reload app)

---

## 🧪 Тестирование:

После перезагрузки попробуйте:

### 1. Логин
Попробуйте войти с существующими учетными данными:
```
Email: test@example.com
Password: Test123!
```

### 2. Регистрация
Создайте новый аккаунт:
- Email (валидный формат)
- Password (минимум 6 символов)
- Имя и Фамилия (опционально)

---

## ✅ Ожидаемый результат:

После применения исправлений:

✅ **Логин работает** - вход в систему успешен  
✅ **Регистрация работает** - можно создать аккаунт  
✅ **Нет ошибки 401** - backend принимает запросы  
✅ **Токен сохраняется** - остаётесь авторизованными

---

## 🔍 Проверка в логах:

После попытки логина в логах Metro должно быть:

**Было (ошибка):**
```
ERROR  [API] Error 401: {"status": 401, "title": "Unauthorized", ...}
```

**Стало (успех):**
```
LOG  [API] POST http://192.168.1.69:5233/api/account/login
LOG  [API] Success: OK
```

---

## 🆘 Если не работает:

### 1. Проверьте, что Metro перезапущен
В терминале должно быть:
```
warning: Bundler cache is empty, rebuilding
```

### 2. Полностью закройте и откройте Expo Go
Не просто reload, а полное закрытие приложения

### 3. Проверьте backend
```bash
netstat -ano | findstr :5233
```
Должен показать процесс на порту 5233

### 4. Проверьте IP адрес
В QR коде должен быть: `exp://192.168.1.69:8081`

---

## 📊 Статус сервисов:

✅ **Backend:** Работает на порту 5233  
✅ **Metro Bundler:** Перезапущен с очисткой кеша  
✅ **Код исправлен:** `src/services/auth.ts` обновлён  
✅ **QR код:** Новый, готов к сканированию

---

## 🎉 Готово!

Отсканируйте новый QR код и попробуйте авторизоваться! 🔐✨

