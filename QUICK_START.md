# Быстрый запуск LeafSide

## 🚀 Запуск бэкенда и мобильного приложения

### Вариант 1: Автоматический запуск (через скрипты)

#### Windows PowerShell:

**Запуск бэкенда:**
```powershell
cd LeafSide-backend\LeafSide.API
dotnet run
```

**Запуск мобильного приложения (в другом терминале):**
```powershell
cd LeafSide.Mobile
npm start
```

### Вариант 2: Ручной запуск

#### 1. Запуск бэкенда

Откройте терминал и выполните:
```powershell
cd C:\Users\User\Desktop\LeafSide_.NET\LeafSide-backend\LeafSide.API
dotnet run
```

Бэкенд будет доступен на:
- **API**: http://localhost:5233
- **Swagger**: http://localhost:5233/swagger

#### 2. Запуск мобильного приложения

Откройте **новый терминал** и выполните:
```powershell
cd C:\Users\User\Desktop\LeafSide_.NET\LeafSide.Mobile
npm start
```

После запуска Expo откроется Metro Bundler с QR-кодом.

### 📱 Запуск на устройстве/эмуляторе

#### Android эмулятор:
```powershell
npm run android
# или
npx expo run:android
```

#### iOS симулятор (только на Mac):
```powershell
npm run ios
# или
npx expo run:ios
```

#### Реальное устройство:
1. Установите **Expo Go** на телефон:
   - [Android](https://play.google.com/store/apps/details?id=host.exp.exponent)
   - [iOS](https://apps.apple.com/app/expo-go/id982107779)

2. Отсканируйте QR-код из терминала

### ⚙️ Настройка .env для реального устройства

Если используете реальное устройство, обновите `.env`:

1. Узнайте IP вашего компьютера:
   ```powershell
   ipconfig
   # Найдите IPv4 адрес (например, 192.168.1.100)
   ```

2. Отредактируйте `.env`:
   ```env
   EXPO_PUBLIC_API_URL=http://192.168.1.100:5233
   ```

3. Перезапустите Expo (`Ctrl+C` и `npm start`)

### ✅ Проверка подключения

После запуска обоих сервисов:

1. **Проверьте бэкенд:**
   ```powershell
   Invoke-WebRequest -Uri http://localhost:5233/api/health
   ```

2. **В мобильном приложении:**
   - Попробуйте зарегистрироваться
   - Просмотрите каталог книг
   - Добавьте книгу в корзину

### 🛑 Остановка сервисов

- **Бэкенд**: Нажмите `Ctrl+C` в терминале бэкенда
- **Expo**: Нажмите `Ctrl+C` в терминале Expo, затем `y` для подтверждения

### 📝 Полезные команды

```powershell
# Проверка статуса бэкенда
Invoke-WebRequest -Uri http://localhost:5233/api/health

# Очистка кэша Expo
npm start -- --clear

# Пересборка бэкенда
cd LeafSide-backend\LeafSide.API
dotnet build

# Проверка установленных зависимостей мобильного приложения
cd LeafSide.Mobile
npm list --depth=0
```

