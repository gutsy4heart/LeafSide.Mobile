# Инструкция по настройке .env файла

## ✅ Правильное содержимое файла .env

Создайте или откройте файл `.env` в корне проекта `LeafSide.Mobile` и добавьте **ТОЛЬКО ЭТУ СТРОКУ**:

```
EXPO_PUBLIC_API_URL=http://192.168.1.69:5233
```

**ВАЖНО:**
- ✅ Без пробелов вокруг `=`
- ✅ Без кавычек
- ✅ Без лишних символов
- ✅ Только одна строка

## 🔄 После изменения .env файла

**ОБЯЗАТЕЛЬНО перезапустите Expo:**

1. Остановите Expo: нажмите `Ctrl+C` в терминале где запущен Expo
2. Запустите заново с очисткой кэша:
   ```powershell
   npx expo start --clear
   ```

## 📱 Выбор правильного URL

### Для iOS устройства (реальный iPhone/iPad):
```
EXPO_PUBLIC_API_URL=http://192.168.1.69:5233
```
(Замените `192.168.1.69` на IP адрес вашего компьютера)

### Для iOS симулятора (на Mac):
```
EXPO_PUBLIC_API_URL=http://127.0.0.1:5233
```

### Для Android эмулятора:
```
EXPO_PUBLIC_API_URL=http://10.0.2.2:5233
```

## 🔍 Как узнать IP адрес вашего компьютера

**Windows PowerShell:**
```powershell
ipconfig | findstr IPv4
```

Найдите адрес вида `192.168.x.x` или `10.x.x.x`

## ✅ Проверка

После перезапуска Expo проверьте логи. Должно быть:

```
[Config] Using API URL: http://192.168.1.69:5233
[API] GET http://192.168.1.69:5233/api/Books
[API] Success: 8 items
```

Если видите старый URL (`http://10.0.2.2:5233`), значит:
- ❌ .env файл не обновлен правильно
- ❌ Expo не перезапущен после изменения .env

## 🛠️ Если не работает

1. Проверьте, что бэкенд запущен:
   ```powershell
   cd LeafSide-backend\LeafSide.API
   dotnet run
   ```

2. Проверьте доступность API в браузере:
   ```
   http://192.168.1.69:5233/api/Books
   ```

3. Убедитесь, что телефон и компьютер в одной Wi-Fi сети

4. Проверьте файрвол Windows - порт 5233 должен быть открыт

