# 🚀 Запуск Android приложения

## ✅ Сборка завершена успешно!

**APK собран!** Время сборки: ~27 минут (первый раз)

---

## 📱 Способ 1: Запуск на физическом устройстве

### Подготовка:

1. **Включите режим разработчика** на Android устройстве:
   - Настройки → О телефоне
   - Нажмите 7 раз на "Номер сборки"

2. **Включите отладку по USB**:
   - Настройки → Для разработчиков
   - Включите "Отладка по USB"

3. **Подключите телефон к компьютеру**

4. **Проверьте подключение:**
   ```bash
   adb devices
   ```
   Должно показать ваше устройство

### Запуск:

```bash
cd LeafSide.Mobile
run-android-studio.bat
```

**Или вручную:**

```bash
cd LeafSide.Mobile

# Установить JAVA_HOME
set "JAVA_HOME=C:\Program Files\Android\Android Studio\jbr"
set "PATH=%JAVA_HOME%\bin;%PATH%"

# Настроить порты
adb reverse tcp:8081 tcp:8081
adb reverse tcp:5233 tcp:5233

# Запустить
npm run android
```

---

## 💻 Способ 2: Эмулятор Android Studio

### Открыть проект в Android Studio:

1. Запустите Android Studio
2. File → Open
3. Выберите: `LeafSide.Mobile/android`
4. Дождитесь синхронизации Gradle (несколько минут)

### Создать эмулятор:

1. В Android Studio: Tools → Device Manager
2. Create Device
3. Выберите устройство (например, Pixel 5)
4. Выберите System Image (Android 13+)
5. Finish

### Запустить:

1. **Запустите Metro bundler** (в отдельном терминале):
   ```bash
   cd LeafSide.Mobile
   npm start
   ```

2. **В Android Studio:**
   - Выберите эмулятор в списке устройств
   - Нажмите Run (зеленая кнопка ▶️)

---

## 🔧 Способ 3: Установка APK вручную

### Найти APK:

После успешной сборки APK находится здесь:
```
LeafSide.Mobile/android/app/build/outputs/apk/debug/app-debug.apk
```

### Установить:

**Через ADB:**
```bash
adb install android/app/build/outputs/apk/debug/app-debug.apk
```

**Через файловый менеджер:**
1. Скопируйте APK на телефон
2. Откройте файл
3. Разрешите установку из неизвестных источников
4. Установите

---

## 🎨 Обновления дизайна

### Что улучшено:

✅ **Карточки книг:**
- Увеличены размеры (высота 220px)
- Более крупные шрифты (title: 18px, price: 22px)
- Улучшенные тени и свечение
- Более выраженный glassmorphism эффект
- Яркие неоновые бейджи жанров
- Кнопка добавления: 52x52px с яркой тенью

✅ **Главный экран:**
- Увеличенные отступы между карточками (16px)
- Лучшее использование пространства
- Более контрастная цветовая схема

✅ **Placeholder для книг без изображений:**
- Красивый градиент
- Крупная иконка (42px)
- Неоновая подсветка

---

## ⚙️ Следующая сборка будет быстрее!

Все зависимости скачаны и закешированы:
- ✅ Gradle 8.14.3
- ✅ NDK 27.1.12297006
- ✅ Build Tools 36
- ✅ CMake 3.22.1
- ✅ Kotlin, React Native модули

**Следующий `npm run android` займёт ~2-3 минуты вместо 27!**

---

## 📋 Полезные команды

```bash
# Очистить кеш и пересобрать
npm run android -- --no-build-cache

# Пересобрать только APK
cd android
./gradlew assembleDebug

# Посмотреть логи
adb logcat *:E

# Перезапустить Metro
npm start -- --reset-cache
```

---

## ❓ Проблемы?

### "JAVA_HOME is not set"
→ Используйте `run-android-studio.bat`

### "No connected devices"
→ Проверьте `adb devices`, перезапустите ADB:
```bash
adb kill-server
adb start-server
```

### "Port 8081 already in use"
→ Остановите старый Metro:
```bash
npx react-native-cli stop
# или найдите процесс:
netstat -ano | findstr :8081
taskkill /PID <номер> /F
```

### Приложение запускается но пустой экран
→ Убедитесь что backend запущен на порту 5233
```bash
cd LeafSide-backend/LeafSide.API
dotnet run
```

---

## 🎯 Готово к тестированию!

Приложение готово к запуску на реальном устройстве или эмуляторе!

