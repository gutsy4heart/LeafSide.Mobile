# 💶 Изменение валюты на евро (EUR)

## ✅ Что изменено:

### **Функция `formatCurrency` в `src/utils/format.ts`**

---

## 🔄 Изменения:

### До:
```typescript
export const formatCurrency = (value: number | null | undefined, currency = 'RUB') => {
  // ...
  return new Intl.NumberFormat('ru-RU', {
    style: 'currency',
    currency,
    maximumFractionDigits: 0,  // Без копеек: 1000 ₽
  }).format(value);
}
```

### После:
```typescript
export const formatCurrency = (value: number | null | undefined, currency = 'EUR') => {
  // ...
  return new Intl.NumberFormat('de-DE', {
    style: 'currency',
    currency,
    maximumFractionDigits: 2,  // С центами: 1.000,50 €
  }).format(value);
}
```

---

## 💡 Что это значит:

### 1. **Валюта по умолчанию: EUR (евро)** 💶
Вместо RUB (₽) теперь используется EUR (€)

### 2. **Локаль: de-DE (немецкий формат)**
```
Было:  1 000 ₽
Стало: 1.000,00 €
```

Немецкий формат использует:
- **Точку** для разделения тысяч: `1.000`
- **Запятую** для десятичных: `1.000,50`

### 3. **Десятичные знаки: 2 (центы)**
```
Было:  100 ₽     (без копеек)
Стало: 100,00 €  (с центами)
```

---

## 📱 Где отображается валюта:

### 1. **BookCard** (карточки книг)
```
Было:  1000 ₽
Стало: 1.000,00 €
```

### 2. **BookDetailsScreen** (детали книги)
```
Было:  2500 ₽
Стало: 2.500,00 €
```

### 3. **CartScreen** (корзина)
```
Было:  Итого: 5000 ₽
Стало: Итого: 5.000,00 €
```

### 4. **CartItemRow** (элементы корзины)
```
Было:  1200 ₽ × 2
Стало: 1.200,00 € × 2
```

### 5. **HomeScreen** (главная)
```
Было:  900 ₽
Стало: 900,00 €
```

### 6. **ProfileScreen** (заказы)
```
Было:  Заказ на 3500 ₽
Стало: Заказ на 3.500,00 €
```

### 7. **CheckoutScreen** (оформление)
```
Было:  К оплате: 7000 ₽
Стало: К оплате: 7.000,00 €
```

---

## 🌍 Другие варианты локали для евро:

Если хотите другой формат, можно изменить локаль:

### Французский формат (fr-FR):
```typescript
new Intl.NumberFormat('fr-FR', { ... })
// Результат: 1 000,00 €
```

### Итальянский формат (it-IT):
```typescript
new Intl.NumberFormat('it-IT', { ... })
// Результат: € 1.000,00
```

### Испанский формат (es-ES):
```typescript
new Intl.NumberFormat('es-ES', { ... })
// Результат: 1.000,00 €
```

### Английский (США) формат (en-US):
```typescript
new Intl.NumberFormat('en-US', { ... })
// Результат: €1,000.00
```

---

## 🔧 Как вернуть рубли:

Если нужно вернуть рубли, измените:

```typescript
export const formatCurrency = (value: number | null | undefined, currency = 'RUB') => {
  // ...
  return new Intl.NumberFormat('ru-RU', {
    style: 'currency',
    currency,
    maximumFractionDigits: 0,
  }).format(value);
}
```

---

## 🧪 Примеры форматирования:

### С копейками/центами:
```typescript
formatCurrency(1234.56)
// Было: 1 235 ₽
// Стало: 1.234,56 €
```

### Целые числа:
```typescript
formatCurrency(1000)
// Было: 1 000 ₽
// Стало: 1.000,00 €
```

### Большие суммы:
```typescript
formatCurrency(9999999)
// Было: 9 999 999 ₽
// Стало: 9.999.999,00 €
```

### Null/Undefined:
```typescript
formatCurrency(null)
// Результат: "Цена не указана"
```

---

## 📋 Backend:

**Важно!** Убедитесь, что backend возвращает цены в **евро**, а не в рублях!

Если backend всё ещё возвращает цены в рублях, их нужно конвертировать:

```typescript
// Пример конвертации (примерный курс):
const priceInEUR = priceInRUB / 100; // 1 EUR ≈ 100 RUB
```

Или обновите данные в базе данных на евро.

---

## ✅ Готово!

Теперь **все цены** в приложении отображаются в **евро (€)** с немецким форматированием! 💶✨

---

## 🔄 Применение изменений:

Metro bundler будет перезапущен с новыми настройками.

**Отсканируйте новый QR код и проверьте!** 📱

